# Telegram SDK Wrapper (Python)

A lightweight Python wrapper for the [Telegram Bot API](https://core.telegram.org/bots/api).  
Send messages, get updates, and build bots with clean, simple code.

---

## 🚀 Installation

bash
pip install requests
